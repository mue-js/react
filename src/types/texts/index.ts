import type { CSSProperties, ReactNode } from 'react'

export interface CommonTextProps {
    children?: ReactNode
    className?: string
    color?: string
    content?: string
    style?: CSSProperties
}
